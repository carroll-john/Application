name: Eligibility Contract

on:
  pull_request:
    paths:
      - "api/evaluate-transcript-eligibility.ts"
      - "api/evaluate-transcript-eligibility.test.ts"
      - "api/__fixtures__/transcript-service-contract.json"
      - "src/lib/eligibility/**"
      - "package.json"
      - "package-lock.json"
  push:
    branches:
      - master
      - "codex/**"
    paths:
      - "api/evaluate-transcript-eligibility.ts"
      - "api/evaluate-transcript-eligibility.test.ts"
      - "api/__fixtures__/transcript-service-contract.json"
      - "src/lib/eligibility/**"
      - "package.json"
      - "package-lock.json"

permissions:
  contents: read

jobs:
  eligibility-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run eligibility contract suite
        run: npm run test:eligibility-contract
